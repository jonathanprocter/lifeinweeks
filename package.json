{
  "name": "lifeinweeks",
  "version": "1.0.0",
  "description": "Life in weeks visualization",
  "scripts": {
    "build": "esbuild weeks_of_life_app.tsx --bundle --format=iife --global-name=WeeksOfLife --outfile=docs/app.js --minify",
    "start": "serve docs -l 3000"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "serve": "^14.2.0"
  }
}
